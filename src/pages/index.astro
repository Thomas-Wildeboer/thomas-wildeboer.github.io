---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Story from "../components/Story.astro";
import SectionTitle from "../components/SectionTitle.astro";
import { persona } from "../persona";
import List from "../components/List.astro";
import ListItem from "../components/ListItem.astro";
import Section from "../components/Section.astro";
const fullName = persona.firstName + " " + persona.secondName;
---

<Layout title={fullName} desc={persona.description}>
  <Header title={fullName} description={persona.description} />
  <Story story={persona.story} />
  <Section>
    <!-- 
    <SectionTitle title="projects" />
    <List>
      {
        persona.sections.projects.map((e) => (
          <ListItem title={e.title} desc={e.desc} url={e.url} />
        ))
      }
    </List>
    -->
    <SectionTitle title="affiliations" />
    <List>
      {
        persona.sections.affiliations.map((e) => (
          <ListItem title={e.title} url={e.url} desc="" />
        ))
      }
    </List>
    <SectionTitle title="personal links" />
    <List>
      {
        persona.sections.links.map((e) => (
          <ListItem title={e.title} url={e.url} desc="" />
        ))
      }
    </List>
  </Section>
</Layout>
